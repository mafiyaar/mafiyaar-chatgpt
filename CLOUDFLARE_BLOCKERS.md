# Cloudflare Blockers

## Test-environment limitation

The configured npm registry repeatedly returned HTTP 503 and direct public npm DNS was unavailable. This prevented installation of Wrangler, the Workers Vitest integration, Vitest, esbuild and Playwright packages, and prevented generation of a trustworthy npm lockfile. Source implementation and dependency-free verification continued.

## External Cloudflare authentication required

Public staging resource creation, remote D1 migration, secret upload and Worker deployment require `wrangler login` or an authorized API token. No URL or remote migration is claimed.

## Product-validation action requiring real people

The six-phone mixed iPhone/Android Equal Action observation remains pending.

## Optional owner action

Custom domain attachment remains optional after workers.dev staging passes.
