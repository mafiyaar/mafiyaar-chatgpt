# Intermediate Audit

The Cloudflare workspace is isolated and self-contained. The Architecture Lock hash matches. The Worker, D1 repository, authentication service, random Durable Object routing, HMAC room directory, opaque tickets, SQLite room snapshot/journal, hibernatable sockets, alarm model, archive writer, Static Assets frontend and PWA have been implemented.

Credential-free TypeScript, static-policy, engine-contract, leak, security, deterministic Equal Action and frontend-build checks pass. Official Workerd/Wrangler/Vitest/Playwright execution remains pending dependency installation because the available npm registry returned HTTP 503 during this environment session.
