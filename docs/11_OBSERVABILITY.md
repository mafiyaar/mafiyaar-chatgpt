# 11 — Observability

Safe structured categories cover request IDs, environment/version, room lifecycle, alarm outcome, archival outcome and reconnection count. Logs omit credentials and hidden state. `/health` reports runtime architecture; `/version` reports app/protocol/minimum-client compatibility.
