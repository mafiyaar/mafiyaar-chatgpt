# 02 — Platform Adapters

Platform-neutral packages define contracts, engine, validation and copy. Cloudflare adapters implement global repository, session repository, archive writer, room persistence, room transport, deadline scheduling, clock and cryptography. The engine imports no Worker, D1, Durable Object, Node HTTP, Node WebSocket or browser API.
