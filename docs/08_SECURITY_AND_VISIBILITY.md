# 08 — Security and Visibility

Server-only: random object ID, role map, seed, target-order map, unresolved actions, predictions, votes, journal and full state. Player-visible messages are generated independently through `viewFor`. Leak scans prohibit object IDs, seeds, complete role maps, other-player private views, unrevealed votes, unresolved actions, completion counts, secrets and debug truth.
