# 05 — D1 Schema

The initial migration creates accounts, profiles, sessions, recovery tokens, HMAC room directory, connection tickets, completed archives, player outcomes, public events, votes, resolved predictions, idempotent statistics applications, statistics, audit events and rate-limit counters. It intentionally contains no live role map, seed, unresolved action or unrevealed vote column.
