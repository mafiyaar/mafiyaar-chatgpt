import {test,expect} from '@playwright/test';
test('first account, responsive PWA shell, create room, and route refresh',async({page}:any)=>{await page.goto('/');await expect(page.locator('.wordmark')).toContainText('MafiYaar');await page.getByRole('button',{name:'Create'}).click();const suffix=Date.now().toString(36);await page.locator('input[name=displayName]').fill(`Browser ${suffix}`);await page.locator('input[name=username]').fill(`browser_${suffix}`);await page.locator('input[name=pin]').fill('864209');await page.getByRole('button',{name:'Create account'}).click();await expect(page.getByText('Aaj kis par shak hai?')).toBeVisible();await page.getByRole('button',{name:/Create room/i}).click();await page.locator('input[name=playerLimit]').fill('6');await page.locator('input[name=mafiaCount]').fill('1');await page.getByRole('button',{name:'Room banao'}).click();await expect(page.getByText(/ROOM CODE|WAITING|room/i).first()).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true);await page.reload();await expect(page.locator('.brand')).toContainText('MafiYaar');});
test('SPA public routes refresh through Workers Static Assets',async({page}:any)=>{for(const route of ['/how','/privacy','/install']){await page.goto(route);expect(await page.locator('body').innerText()).toContain('MafiYaar');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true);}});
